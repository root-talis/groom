openapi: 3.1.0
info:
  title: TODO example (Groom)
  description: Shows how to structure backend with three-layer architecture and generate spec for frontend.
  contact:
    name: name
    email: mail@example.com
  license:
    name: MIT OR Apache-2.0
    identifier: MIT OR Apache-2.0
  version: 0.0.1
paths:
  /tasks:
    get:
      summary: Lists tasks
      parameters:
      - name: title
        in: query
        required: false
        schema:
          type:
          - string
          - 'null'
      - name: status
        in: query
        required: false
        schema:
          type:
          - array
          - 'null'
          items:
            $ref: '#/components/schemas/Status'
      - name: sort_by
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/TaskOrderBy'
      - name: order
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/OrderDirection'
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskList'
        '500':
          description: ''
    post:
      summary: Adds a new task.
      requestBody:
        description: Request body
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddTaskRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AddTaskRequest'
        required: true
      responses:
        '200':
          description: Task added successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskViewModel'
        '400':
          description: Malformed request, e.g. missing title or title is too long.
          content:
            application/json:
              schema:
                type: string
        '409':
          description: Task already exists with the same title
        '500':
          description: Unexpected error when adding a task, e.g. database is down or serialization error.
  /tasks/{task_id}:
    get:
      summary: Gets a single task.
      parameters:
      - name: task_id
        in: path
        required: true
        schema:
          type: integer
          format: int64
          minimum: 0
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskViewModel'
        '404':
          description: ''
        '500':
          description: ''
  /tasks/{task_id}/name:
    put:
      summary: Renames a task.
      parameters:
      - name: task_id
        in: path
        required: true
        schema:
          type: integer
          format: int64
          minimum: 0
      requestBody:
        description: Request body
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RenameTaskRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/RenameTaskRequest'
        required: true
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskViewModel'
        '400':
          description: ''
          content:
            application/json:
              schema:
                type: string
        '404':
          description: ''
        '409':
          description: ''
        '500':
          description: ''
  /tasks/{task_id}/status/cancel:
    put:
      summary: Mark the task as cancelled.
      parameters:
      - name: task_id
        in: path
        required: true
        schema:
          type: integer
          format: int64
          minimum: 0
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskViewModel'
        '404':
          description: ''
        '409':
          description: ''
        '500':
          description: ''
  /tasks/{task_id}/status/done:
    put:
      summary: Mark the task as done.
      parameters:
      - name: task_id
        in: path
        required: true
        schema:
          type: integer
          format: int64
          minimum: 0
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskViewModel'
        '404':
          description: ''
        '409':
          description: ''
        '500':
          description: ''
  /tasks/{task_id}/status/pending:
    put:
      summary: Mark the task as pending.
      parameters:
      - name: task_id
        in: path
        required: true
        schema:
          type: integer
          format: int64
          minimum: 0
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskViewModel'
        '404':
          description: ''
        '409':
          description: ''
        '500':
          description: ''
components:
  schemas:
    AddTaskRequest:
      type: object
      description: Request body
      required:
      - title
      properties:
        title:
          type: string
    OrderDirection:
      type: string
      enum:
      - Asc
      - Desc
    RenameTaskRequest:
      type: object
      description: Request body
      required:
      - title
      properties:
        title:
          type: string
    Status:
      type: string
      enum:
      - Pending
      - Done
      - Cancelled
    TaskList:
      type: array
      items:
        $ref: '#/components/schemas/TaskViewModel'
    TaskOrderBy:
      type: string
      enum:
      - Id
      - Title
      - Status
    TaskViewModel:
      type: object
      required:
      - id
      - title
      - status
      properties:
        id:
          type: integer
          format: int64
          minimum: 0
        status:
          $ref: '#/components/schemas/Status'
        title:
          type: string
